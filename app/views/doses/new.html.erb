<div class="container mt-4">
  <h3><%= @cocktail.name %></h3>
  <%= simple_form_for([@cocktail, @dose]) do |f| %>
    <div class="form-group">
      <%= f.input :description, label: "Please enter dose description", placeholder: "1 shot", class: "form-control" %>
    <div>
    <div class="form-group">
      <%= f.association :ingredient, label: "Select ingredient", class: "form-control" %>
      <%# <%= link_to('Add new', '#', class: "btn btn-primary btn-block") %>
      <%= f.submit 'Add new dose', class: "btn btn-primary btn-block" %>
      <div>
  <% end %>
<div>
